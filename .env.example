POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=financial
POSTGRES_PORT=5432
API_PORT=8080
WEB_PORT=3000
DATABASE_URL=Host=postgres;Port=5432;Database=financial;Username=postgres;Password=changeme
# Used by Next.js rewrites when running the web app outside Docker (npm run dev).
API_URL=http://localhost:8080
# Opcional: chave da Anthropic ou OpenAI para categorização via LLM (uma das duas chega).
# Se ambas estiverem definidas, OpenAI tem prioridade.
# ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-proj-...
# LLM_MODEL=gpt-4o-mini   # opcional, sobrescreve o modelo padrão
